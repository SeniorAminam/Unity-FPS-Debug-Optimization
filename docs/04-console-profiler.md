# 📊 Console و Profiler

<div align="center">

**استفاده حرفه‌ای از ابزارهای تحلیل یونیتی**

</div>

---

## 🖥️ Console Window

### باز کردن
```
Window > General > Console
یا Ctrl+Shift+C
```

### بخش‌های Console

```
┌─────────────────────────────────────────────────────────────┐
│ Console                                              □ × ─ │
├─────────────────────────────────────────────────────────────┤
│ [Clear] [Collapse] [Clear on Play] [Error Pause]    🔍     │
│ [⚪ 125] [🟡 3] [🔴 1]                                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ⚪ [NetworkManager] Connected to Photon                    │
│ ⚪ [NetworkManager] Joined lobby: DefaultLobby             │
│ ⚪ [NetworkManager] Player "Amin" joined room "TestRoom"   │
│ 🟡 [PlayerHealth] TakeDamage called with 0 damage          │
│ 🔴 NullReferenceException: Object reference not set...     │
│    at PlayerHealth.TakeDamage (damage=20)                  │
│    at FpsGun.Shoot() (Line 79)                             │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ NullReferenceException: Object reference not set to an     │
│ instance of an object                                       │
│ PlayerHealth.TakeDamage (Int32 amount, String enemyName)   │
│ (at Assets/Scripts/PlayerHealth.cs:91)                     │
└─────────────────────────────────────────────────────────────┘
```

### گزینه‌های مهم

| گزینه | کاربرد |
|-------|--------|
| **Clear** | پاک کردن تمام لاگ‌ها |
| **Collapse** | ادغام لاگ‌های تکراری |
| **Clear on Play** | پاک کردن هنگام شروع Play |
| **Error Pause** | ⭐ توقف بازی هنگام خطا |
| **Editor** | نمایش لاگ‌های Editor |
| **Player** | نمایش لاگ‌های Build |

### فیلتر کردن

```
🔍 Search Box:
├── "Player"      → لاگ‌های حاوی "Player"
├── "t:error"     → فقط خطاها
├── "t:warning"   → فقط هشدارها
└── "[Network"    → لاگ‌های شبکه
```

---

## 📈 Unity Profiler

### باز کردن
```
Window > Analysis > Profiler
یا Ctrl+7
```

### نمای کلی Profiler

```
┌─────────────────────────────────────────────────────────────┐
│ Profiler                                            □ × ─ │
├─────────────────────────────────────────────────────────────┤
│ [▶ Record] [Frame: 1234] [< >] [Deep Profile] [Call Stacks]│
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ CPU Usage ════════════════════════════════════════════════ │
│ ████████████████████████                    16.7ms (60 FPS)│
│ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │
│ │   Scripts    │Physics│ Rendering │   Other   │  Idle   │ │
│                                                             │
│ GPU Usage ════════════════════════════════════════════════ │
│ ██████████████████████████████████████████████      14.2ms │
│                                                             │
│ Memory ═══════════════════════════════════════════════════ │
│ Total: 512 MB | Used: 156 MB | GC: 2.1 MB                  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ Hierarchy                                                   │
│ ┌──────────────────────────────────────────────────────────┤
│ │ Overview    │ Time ms │ GC Alloc │ Calls │              │
│ ├──────────────────────────────────────────────────────────┤
│ │ PlayerLoop  │  16.7   │   0 B    │   1   │              │
│ │ ├─Update    │   8.3   │  256 B   │  45   │ ⚠️           │
│ │ │ ├─FpsGun  │   2.1   │  128 B   │   1   │              │
│ │ │ └─Network │   4.2   │  128 B   │  12   │              │
│ │ └─LateUpdate│   3.1   │   0 B    │  23   │              │
│ └──────────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 CPU Profiler

### ستون‌های مهم

| ستون | توضیح | هدف |
|------|-------|-----|
| **Time ms** | زمان اجرا | کمتر از 16.67ms برای 60 FPS |
| **Self ms** | زمان بدون فرزندان | شناسایی hotspot |
| **Calls** | تعداد فراخوانی | کاهش تعداد |
| **GC Alloc** | حافظه تخصیص‌یافته | ⭐ باید 0 باشد! |

### شناسایی مشکلات

```csharp
// ❌ مشکل: GC Allocation در Update
void Update() {
    string status = "Player: " + playerName + " HP: " + health;  // ⚠️ GC!
    Debug.Log(status);  // ⚠️ GC!
    
    var enemies = FindObjectsOfType<Enemy>();  // ⚠️ GC HEAVY!
}

// ✅ راه‌حل: حذف Allocation
private StringBuilder statusBuilder = new StringBuilder();
private Enemy[] cachedEnemies;

void Start() {
    cachedEnemies = FindObjectsOfType<Enemy>();  // فقط یکبار
}

void Update() {
    statusBuilder.Clear();
    statusBuilder.Append("Player: ");
    statusBuilder.Append(playerName);
    statusBuilder.Append(" HP: ");
    statusBuilder.Append(health);
    // فقط در Debug Mode
    #if UNITY_EDITOR
    Debug.Log(statusBuilder.ToString());
    #endif
}
```

### Frame Time Budget

```
┌─────────────────────────────────────────────────────────────┐
│                    FRAME TIME BUDGET                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Target FPS    │  Budget per Frame                         │
│  ──────────────┼────────────────────────────────────────── │
│  30 FPS        │  33.33 ms  ████████████████████████████   │
│  60 FPS        │  16.67 ms  ██████████████                 │
│  90 FPS (VR)   │  11.11 ms  █████████                      │
│  120 FPS       │   8.33 ms  ██████                         │
│  144 FPS       │   6.94 ms  █████                          │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  💡 TIP: برای 60 FPS:                                      │
│     - Scripts: ~5 ms                                        │
│     - Physics: ~3 ms                                        │
│     - Rendering: ~8 ms                                      │
│     - Buffer: ~0.67 ms                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 💾 Memory Profiler

### نصب Memory Profiler Package

```
Window > Package Manager > Memory Profiler
```

### Simple Memory View

```
┌─────────────────────────────────────────────────────────────┐
│                    MEMORY SNAPSHOT                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Category          │ Size      │ Count                      │
│  ──────────────────┼───────────┼──────────                  │
│  Textures          │  89.2 MB  │  124                       │
│  Meshes            │  23.4 MB  │   87                       │
│  Audio             │  12.1 MB  │   16                       │
│  Scripts           │   4.2 MB  │  156                       │
│  Particles         │   2.8 MB  │   45                       │
│  ──────────────────┼───────────┼──────────                  │
│  Total Committed   │ 156.2 MB  │                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### شناسایی Memory Leaks

```csharp
// ❌ مثال Memory Leak در ImpactLifeCycle
public class ImpactLifeCycle : MonoBehaviour {
    void Start() {
        GetComponent<ParticleSystem>().Play();
        // Destroy(gameObject, 1.5f);  // اگر فراموش شود = LEAK!
    }
}

// ✅ بررسی در Profiler:
// 1. شروع بازی
// 2. تیراندازی چند بار
// 3. بررسی تعداد impactXXX در Hierarchy
// 4. اگر افزایش پیدا کرد = LEAK!
```

---

## 🔴 شناسایی Spikes

### Spike چیست؟

```
Frame Time Graph:
                    ↓ SPIKE!
16ms ──────────────────────────────────────────────────
     ╭─╮ ╭─╮ ╭─╮ ╭─────╮ ╭─╮ ╭─╮ ╭─╮ ╭─╮ ╭─╮ ╭─╮ ╭─╮
     │ │ │ │ │ │ │     │ │ │ │ │ │ │ │ │ │ │ │ │ │ │
     ╰─╯ ╰─╯ ╰─╯ │     │ ╰─╯ ╰─╯ ╰─╯ ╰─╯ ╰─╯ ╰─╯ ╰─╯
                 │     │
50ms ────────────│     │────────────────────────────────
                 ╰─────╯
```

### علل رایج Spikes

| علت | راه‌حل |
|-----|--------|
| **Garbage Collection** | Object Pooling |
| **Load Asset** | Async Loading |
| **Instantiate** | Pre-warming |
| **Physics Sync** | Fixed Timestep |
| **Shader Compile** | Shader Warmup |

### بررسی در Profiler

```
۱. در Profiler روی Spike کلیک کنید
۲. به Hierarchy بروید
۳. دنبال بزرگترین "Time ms" بگردید
۴. عموماً یکی از این‌هاست:
   - GC.Collect
   - Loading.ReadObject
   - Instantiate
   - Physics.SyncColliders
```

---

## 📱 Profiling در Device

### Remote Profiling

```
۱. Build Settings > Development Build ✅
۲. Build Settings > Autoconnect Profiler ✅
۳. Build و اجرا روی Device
۴. در Unity: Profiler > Connected Player > [Device Name]
```

### Deep Profile

```
⚠️ هشدار: Deep Profile خیلی کند است!

استفاده:
۱. Profiler > Deep Profile ✅
۲. همه متدها و خطوط قابل مشاهده می‌شوند
۳. برای شناسایی دقیق Bottleneck استفاده کنید
۴. بعد از استفاده خاموش کنید!
```

---

## 🔧 Timeline View

### مشاهده موازی

```
┌─────────────────────────────────────────────────────────────┐
│ Timeline View                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ Main Thread  │▓▓▓▓▓▓▓│░░░░│▓▓▓▓▓▓▓▓▓▓│░░░░░░░│▓▓▓│        │
│              │Update │Wait│Rendering  │  Idle  │Late│        │
│                                                             │
│ Render Thread│░░░░░░░░░░░░│▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓│░░░░░░│        │
│              │    Wait    │   Draw Calls   │ Sync │        │
│                                                             │
│ Job Worker 1 │░░░░│▓▓▓▓▓▓▓▓│░░░░░░░░░░░░░░░░░░░░░░│        │
│              │Wait│Physics │         Idle         │        │
│                                                             │
│ Job Worker 2 │░░░░│▓▓▓▓▓▓▓▓│░░░░░░░░░░░░░░░░░░░░░░│        │
│              │Wait│Navmesh │         Idle         │        │
│                                                             │
└─────────────────────────────────────────────────────────────┘

Legend:
▓▓▓ = کار در حال انجام
░░░ = در انتظار
```

---

## 🎮 مثال عملی: پروفایل FpsGun

### قبل از بهینه‌سازی

```
FpsGun.Shoot()
├── Time: 2.1 ms
├── GC Alloc: 128 B  ⚠️
└── Calls: 1

Breakdown:
  ├── CrossPlatformInputManager.GetButton: 0.1 ms
  ├── Physics.Raycast: 0.8 ms
  ├── PhotonNetwork.Instantiate: 0.9 ms  ⚠️ (بیشترین)
  └── StartCoroutine: 0.3 ms (128 B GC)  ⚠️
```

### مشکلات شناسایی شده

```csharp
// 1️⃣ مشکل: Instantiate هر بار تیراندازی
PhotonNetwork.Instantiate("impactFlesh", ...);

// 2️⃣ مشکل: StartCoroutine allocation
StartCoroutine(DisableShootingEffect());

// 3️⃣ مشکل: new Vector3 هر فریم
new Vector3(Screen.width/2, Screen.height/2, 0f)
```

### بعد از بهینه‌سازی

```csharp
// 1️⃣ راه‌حل: Object Pooling
ImpactPool.Get("impactFlesh").Spawn(position, rotation);

// 2️⃣ راه‌حل: Cache Coroutine
private WaitForSeconds disableDelay = new WaitForSeconds(0.05f);
yield return disableDelay;

// 3️⃣ راه‌حل: Cache Vector
private Vector3 screenCenter;
void Awake() {
    screenCenter = new Vector3(Screen.width/2f, Screen.height/2f, 0f);
}
```

---

## 📋 Checklist بررسی عملکرد

```
□ FPS پایدار 60+ هست؟
□ GC Alloc در Update صفر است؟
□ Spike های غیرعادی نداریم؟
□ Memory مصرفی منطقی است؟
□ Draw Calls کمتر از 100؟
□ Tri Count کمتر از 100K؟
```

---

## 🚀 بخش بعدی

در بخش بعدی، با **دیباگ شبکه و Photon** آشنا می‌شویم.

<div align="center">

**[⏮️ بخش قبلی](./03-debugging-tools.md)** | **[⏭️ بخش بعدی: دیباگ شبکه](./05-debugging-network.md)**

</div>

---

<div align="center">

*Developed by Amin Davodian*

</div>
